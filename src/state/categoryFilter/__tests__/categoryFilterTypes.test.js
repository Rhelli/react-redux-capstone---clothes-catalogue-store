import FILTER_CATEGORY from '../categoryFilterTypes';

describe('The categoryFilterTypes hard-coded variable', () => {
  it("Returns the string 'FILTER_CATEGORY' when called", () => {
    expect(FILTER_CATEGORY).toEqual('FILTER_CATEGORY');
  });
});
